# 02 Workflow是什么

Workflow是把任务拆成若干处理步骤，再通过变量和连线，按预设顺序或条件重复执行的流程，可理解为“会运行的流程图”。

三个适用信号：

1. 任务不止一步；
2. 不同输入需要不同路径；
3. 有明确边界、兜底或人工确认。

简单润色、单段总结等一次性任务通常不需要Workflow。Workflow路径主要由产品预设；Agent具有更高的自主决策空间，本课不展开。

