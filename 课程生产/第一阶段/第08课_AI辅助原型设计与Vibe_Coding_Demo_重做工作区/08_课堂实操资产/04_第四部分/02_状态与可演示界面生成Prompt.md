# 状态与可演示界面生成 Prompt

```text
请先根据正式PRD、产品决策、页面信息架构、用户流程和低保真评审记录生成页面状态表。

每个状态包含：触发条件、页面反馈、用户动作、系统结果和产品依据。
至少覆盖数量不足、数量上限、跨类目、参数缺失、参数冲突、商品下架和返回恢复。

不得推测数据，不得增加AI推荐、云端保存或统一到手价。待确认内容必须单独标记。

得到确认后，再基于low-fi.html生成prototype/core-ui.html，提升信息层级、可读性、操作可发现性和状态辨识度，并逐项检查状态表。
```
